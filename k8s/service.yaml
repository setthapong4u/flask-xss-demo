apiVersion: v1
kind: Service
metadata:
  name: flask-xss-demo-svc
spec:
  type: NodePort
  selector:
    app: flask-xss-demo
  ports:
    - port: 80          # Service port
      targetPort: 80    # ContainerPort in the pod
      nodePort: 30080   # NodePort on your Ubuntu host
